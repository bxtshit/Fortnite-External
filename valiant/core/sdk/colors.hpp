#pragma once

#include "../../dependencies/imgui/imgui.h"

class colors
{
public:

	const ImColor white_color = ImColor(255, 255, 255);

	const ImColor black_color = ImColor(0, 0, 0);

	const ImColor red_color = ImColor(255, 0, 8);

	const ImColor blue_color = ImColor(0, 0, 255);

	const ImColor yellow_color = ImColor(251, 255, 0);

	const ImColor green_color = ImColor(0, 255, 13);
};